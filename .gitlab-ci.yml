image: node:20-alpine

stages:
  - build

before_script:
  - npm install

build:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - .next
      - public
    expire_in: 1 day
  only:
    - main
    - develop